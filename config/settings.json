{
  "galaxy.meteor.com": {
    "env": {
      "MONGO_URL": "mongodb://${MONGO_LOGIN}:${MONGO_PASSWORD}@${MONGO_HOSTNAME}:${MONGO_PORT}/${MONGO_DATABASE}",
      "ROOT_URL": "http://casebook.meteorapp.com"
    }
  }
}
